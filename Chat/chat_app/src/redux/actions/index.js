// import * as actionTypes from '../utils/actionTypes';

// export const sendMessage = (message) => ({
//     type: actionTypes.INPUT_MESSAGE,
//     message,
// });

import {messages} from './messages';

export const actions = {
    messages,
};
